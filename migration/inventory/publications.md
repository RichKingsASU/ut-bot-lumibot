# Publications / logical replication

    supabase_realtime        all_tables=False i/u/d/t=True/True/True/True
    supabase_realtime_messages_publication all_tables=False i/u/d/t=True/True/True/True

Inspected only. No slot or publication was created, altered or dropped.

A publication exists, so logical replication is feasible as a Cloud SQL migration path (Method B) rather than dump/restore only.
