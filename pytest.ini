[pytest]
testpaths = tests backtests/tests gemini-computer-use-agent/tests test_logic.py
addopts = --strict-markers
markers =
    slow: heavyweight or long-running tests
    cloud: tests requiring cloud infrastructure
    agent: agent component tests
    backtest: backtest mechanics tests
